<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- state_pressed为判断是否被按下，如果值为false，说明未被按下，背景为btn_shape -->
    <item android:drawable="@drawable/button_normal" android:state_pressed="false"></item>
    <!-- state_pressed为判断是否被按下，如果值为true，说明被按下，背景为btn_shape_after -->
    <item android:drawable="@drawable/button_select" android:state_pressed="true"></item>

</selector>
